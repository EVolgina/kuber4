apiVersion: v1
kind: Pod
metadata:
  name: multitool-pod
spec:
  containers:
  - name: multitool-container
    image: wbitt/network-multitool:latest
    command: ["network-multitool", "--port", "8080", "/bin/sleep", "infinity"]
    ports:
    - containerPort: 8080
